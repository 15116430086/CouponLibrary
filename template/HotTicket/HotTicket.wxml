<template name="hotTicketBox">
  <block wx:for="{{ListCoupon_ReleaseQuery}}" wx:key="ReleaseID">
    <view class="hotboxH">
      <view class="hotblockH">
        <view class="ticket-boxH">
          <image class="ticket-imgH" src="{{ImageOne}}" />
          <view class="ticket-mesH">
            <view class="ticket-ul-pH">
              <view class="ticket-mes-pH">{{item.CouponName}}啦啦啦啦</view>
              <view class="ticket-mesmesH ">{{GroupName}}</view>
            </view>

            <view class="ticket-mes-ulH">
              <view class="one">限量{{item.ReleaseNUM}}份</view>|
              <view class="two">{{item.ReceiveNUM}}已领</view>
            </view>
          </view>
        </view>
        <view wx:if="{{item.SalePrice>0}}" class="hotbtnH" data-ReleaseID="{{item.ReleaseID}}" data-CouponID="{{item.CouponID}}" bindtap="onBindReceiveTap">立即领取</view>
        <view wx:else class="hotbtnH" data-ReleaseID="{{item.ReleaseID}}" data-CouponID="{{item.CouponID}}" bindtap="onBindReceiveTap">免费领取</view>    
      </view>
    </view>
  </block>
</template>